type Category = {
    name: string;
    description: string;
    shortDescription: string;
    id: string;
    createdAt: string;
    updatedAt: string;
    deletedAt: string;
};

export default Category;
